# Runtime-only dependencies for the FastAPI inference app.
# Dev, notebook, and training-only deps live in requirements.txt.

# Web framework
fastapi==0.128.0
starlette==0.50.0
uvicorn==0.40.0
python-multipart==0.0.22

# Inference — pure NumPy. scikit-learn/scipy/joblib are NOT runtime deps; the
# model is served from exported weights via modeling/lite_model.py. They are
# only used offline to train and to export those weights (see requirements.txt
# and scripts/export_lite_model.py).
numpy==2.4.1

# Templates
Jinja2==3.1.6
MarkupSafe==3.0.3

# Config / logging
loguru==0.7.3
python-dotenv==1.2.1

# Pydantic (FastAPI dep)
pydantic==2.12.5
pydantic_core==2.41.5
annotated-types==0.7.0
typing-inspection==0.4.2
typing_extensions==4.15.0

# Async I/O (starlette dep)
anyio==4.12.1
idna==3.11

# HTTP server (uvicorn dep)
h11==0.16.0
click==8.3.1

packaging==26.0
